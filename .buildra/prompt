This is a Vue 3 + Vite + TypeScript project with Tailwind CSS.

STYLING RULES:
- Use Tailwind CSS utility classes for ALL styling in <template> blocks
- Do NOT use <style scoped> blocks for layout/spacing — use Tailwind classes
- Follow mobile-first responsive design (sm:, md:, lg: breakpoints)

IMPORTANT:
- Always run `npm install && npm run dev` to start
- Use Composition API with <script setup lang="ts">
- Never use `vite` directly — always use `npm run dev`
